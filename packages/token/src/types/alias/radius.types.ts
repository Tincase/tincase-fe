import { SizeProperty } from '../primitive';

// prettier-ignore
export type Radius = {
  none:           SizeProperty;
  sm:             SizeProperty;
  md:             SizeProperty;
  lg:             SizeProperty;
  max:            SizeProperty;
}
